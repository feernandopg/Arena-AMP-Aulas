# Dependências para RODAR e COMPILAR o app instalável (no SEU PC de build)
flask
flask-sqlalchemy
flask-login
werkzeug
cryptography
nuitka
# Janela nativa (sem a barra do Edge). No Windows puxa o pythonnet + usa o
# WebView2. Se por algum motivo não empacotar/rodar, o app cai pro Edge --app.
pywebview
